; Material uploads (see MaterialRequest) allow up to 20MB files — PHP's
; own defaults (2M/8M) reject anything past that before Laravel's
; validation even runs, surfacing as a generic "file failed to upload."
upload_max_filesize = 25M
post_max_size = 25M
